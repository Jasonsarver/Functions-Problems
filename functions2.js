/* 
step.1 define a js function that accepts 
string as parameter
step.2 split the string passed in, turn it into 
array of each letter. store in variable called
arrayOfLetters
step.3 define a variable called numberOfVowels
and set intitial value equal to 0
step.4 define a variable that states what a vowel
is, an array of vowels
step.5 start looping through array of letters
step.6 check if current letter in our loop is 
a vowel
*/

function vowelCount(str) {
    const arrayOfVowels = str.tolowercase().split("")
    let numberOfVowels = 0
    let vowels = ["a","e","i","o","u"];
    for(let i = 0; x < arrayOfVowels.length; i++) {
      if (arrayOfVowels.includes(arrayOfVowels[i])){
        }
    }
      return numberOfVowels;
  }
  